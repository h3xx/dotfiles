#!/bin/bash

MESON_ARGS=(
	# Core options
	--optimization 2
	--buildtype release
	--infodir info
	# Directories
	--localstatedir /var
	--mandir man
	--prefix /usr
	--sharedstatedir /var/lib
	--sysconfdir /etc
)

exec meson "${MESON_ARGS[@]}" "$@"
