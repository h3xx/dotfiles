#!/bin/bash
# vi: et sts=4 sw=4 ts=4

# Open vim with the real path for each non-option argument

VIM_ARGS=()
for ARG; do
    if [[ -e $ARG ]]; then
        ARG=$(realpath -- "$ARG")
    fi
    VIM_ARGS+=("$ARG")
done
exec vim "${VIM_ARGS[@]}"
