#!/bin/bash
# vi: et sts=4 sw=4 ts=4

# Script first conceived 2018-07-12

while [[ $PWD != / ]]; do
    if [[ -e artisan ]]; then
        exec php artisan "$@"
    fi
    cd ..
done

printf 'Failed to find artisan\n' >&2
exit 2
